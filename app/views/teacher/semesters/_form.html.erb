<%= form_for(teacher_semester, url: teacher_semesters_path) do |f| %>
  <% if teacher_semester.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_semester.errors.count, "error") %> prohibited this teacher_semester from being saved:</h2>

      <ul>
      <% teacher_semester.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :start %>
    <%= f.datetime_select :start %>
  </div>

  <div class="field">
    <%= f.label :end %>
    <%= f.datetime_select :end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
