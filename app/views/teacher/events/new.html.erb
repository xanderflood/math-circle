<h1>New Meeting</h1>

<h3>
  Section
  <%= @event.section.description %>
  of
  <%= @event.section.course.name %>
</h3>
<h4>
  Semester
  <%= @event.section.course.semester.name %>
</h4>

<%= render 'form', event: @event %>

<%= link_to 'Back', teacher_section_path(@event.section) %>
