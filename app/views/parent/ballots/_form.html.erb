<%= form_for(ballot, url: @url) do |f| %>
  <% if ballot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ballot.errors.count, "error") %> prohibited this ballot from being saved:</h2>

      <ul>
      <% ballot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :student_id, value: ballot.student.id %>
  </div>

  <div class="field">
    <%= f.label :semester %>
    <%= f.collection_select :semester_id, Semester.all, :id, :name, {}, {disabled: 'diabled'} %>
  </div>

  <div class="field">
    <%= f.label :course %>
    <%= f.collection_select :course_id, Course.all, :id, :name %>
  </div>

  <% if ballot.persisted? %>
    <div class="field">
      <%= f.label :ballot_preferences %>

      <table>
        <thead>
          <tr>
            <th colspan="1">preference</th>
            <th colspan="1">section</th>
          </tr>
        </thead>
e
        <tbody>
          <% @preferences = @ballot.preferences %>
          <% (1..@sections.count).each do |pref| %>
            <tr>
              <td><%= pref %></td>
              <td>
                <%= f.fields_for "preferences[#{pref}]" do |prefs| %>
                  <%= prefs.select :section, options_for_select(@options, @selections[pref-1]) %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
