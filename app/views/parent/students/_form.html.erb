<%= form_for(parent_student) do |f| %>
  <% if parent_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent_student.errors.count, "error") %> prohibited this parent_student from being saved:</h2>

      <ul>
      <% parent_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
